import os
import discord
from discord.ext import commands
import dotenv

async def block_user(user):
    # Aquí puedes implementar la lógica para bloquear al usuario
    print(f"Usuario {user} ha sido bloqueado.")


async def ban_user(user):
    # Aquí puedes implementar la lógica para banear al usuario
    print(f"Usuario {user} ha sido baneado.")
